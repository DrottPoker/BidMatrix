# Foundation plan notice

The earlier dashboard-only plan has been superseded by the authoritative `BIDMATRIX_AI_COMPANY_MASTER_PLAN.md`.

The active bounded target is Foundation Release F0. Implementation must follow its phases in dependency order and record verified status in `docs/implementation-status.md`.

Useful visual direction from the initial Vite prototype has been adapted into the Next.js product shell. The prototype bid scoring demonstration was removed because scoring is an explicit F0 non-goal.
